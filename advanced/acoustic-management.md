# Acoustic Management Configuration（声学管理配置）

用于控制机械硬盘（HDD）噪声的设置。

需要安装机械硬盘才能进行相关设置，否则会提示 HDD not found（找不到机械硬盘）。

## Automatic Acoustic Management（自动声学管理）

选项：

Disable（禁用）

Enable（启用）

说明：

大多数现代硬盘驱动器都能够降低磁头移动速度以减少其噪声输出。可设定的值取决于磁盘，某些磁盘可能不支持此功能。
